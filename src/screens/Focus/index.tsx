import { View, Text } from 'react-native';
import { globalStyles } from '@/styles/global';

export function FocusScreen() {
  return (
    <View style={globalStyles.center}>
      <Text style={globalStyles.textH2}>Foco</Text>
    </View>
  );
}
